package com.vtxlab.project.crypto.coingecko.model.dto;

import com.vtxlab.project.crypto.coingecko.model.cryptoall.price.CryptoRates;

import java.util.HashMap;

public class CryptoRatesMap extends HashMap<String, CryptoRates> {

/*
    // Store the API response (json) as String //
    String jsonString = cryptoApi.invoke(baseUrl, serviceVers, simplePriceUrl, cMap, String.class);
    // Set the returnType of readValue() of objectMapper //
    MapType mapType = objectMapper.getTypeFactory().constructMapType(Map.class, String.class, CryptoRates.class); // = Map<String, CryptoRates>
    Map<String, CryptoRates> cryptoRates = objectMapper.readValue(jsonString, mapType);
*/

}